spring:
  datasource:
    url: jdbc:mysql://8.140.51.188:3306/exam?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: server
    password: we44s@WT551dfS
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
  data:
    redis:
      host: 8.140.51.188
      port: 6379
      password:
      database: 1
      connect-timeout: 10s
      lettuce:
        pool:
          max-active: 200
          max-wait: -1ms
          max-idle: 10
          min-idle: 0

# knife4j的增强配置
knife4j:
  basic:
    # 开启访问权限控制
    enable: false

# mybatisPlus
mybatis-plus:
  configuration:
    #开发环境开启日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
